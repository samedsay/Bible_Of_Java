package main;

public class Solution05_25 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		double pi = 0;
		for (int i = 1; i <= 10000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 10000");

		pi = 0;
		for (int i = 1; i <= 20000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 20000");

		pi = 0;
		for (int i = 1; i <= 30000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 30000");

		pi = 0;
		for (int i = 1; i <= 40000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 40000");

		pi = 0;
		for (int i = 1; i <= 50000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 50000");

		pi = 0;
		for (int i = 1; i <= 60000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 60000");

		pi = 0;
		for (int i = 1; i <= 70000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 70000");

		pi = 0;
		for (int i = 1; i <= 80000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 80000");

		pi = 0;
		for (int i = 1; i <= 90000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 90000");

		pi = 0;
		for (int i = 1; i <= 100000; i++) {
			pi += (Math.pow(-1, i + 1) / (2.0 * i - 1));
		}
		pi *= 4;
		System.out.println("PI is " + pi + " for i = 100000");

	}

}
